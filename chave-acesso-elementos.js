_cokwr - nome
_cpzh4 - faltas
_cre1l - P1
_chk2m - P2
_ciyn3 - P3

/* const faltas = linhas.forEach(row => {
        console.log(row._cpzh4)
    })
    const P1 = linhas.forEach(row => {
        console.log(row._cre1l)
    })
    const P2 = linhas.forEach(row => {
        console.log(row._cre1l)
    })
    const P3 = linhas.forEach(row => {
        console.log(row._ciyn3)
    })*/

 /*   const P1 = await promisify(pagina.getCells)({
        'min-row': 4,
        'max-row': 23,
        'min-col': 4,
        'max-col': 4,
    })
    const P2 = await promisify(pagina.getCells)({
        'min-row': 4,
        'max-row': 23,
        'min-col': 5,
        'max-col': 5,
    })
    const P3 = await promisify(pagina.getCells)({
        'min-row': 4,
        'max-row': 23,
        'min-col': 6,
        'max-col': 6,
    })

    const situcao = await promisify(pagina.getCells)({
        'min-row': 4,
        'max-row': 23,
        'min-col': 7,
        'max-col': 7,
    })

    const notaAprov = await promisify(pagina.getCells)({
        'min-row': 4,
        'max-row': 23,
        'min-col': 8,
        'max-col': 8,
    })

    for(const celula of P1){
        console.log(`${celula.value}`)
        console.log(`----------------`)
    }

    for(const celula2 of P2){
        console.log(`${celula2.value}`)
        console.log(`----------------`)
    }

    for(const celula2 of P3){
        console.log(`${celula2.value}`)
    }
*/